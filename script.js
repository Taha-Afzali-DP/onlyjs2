const login = (user) => {
  if (user.name === "taha") {
    return true;
  } else {
    return false;
  }
};
const register = (user) => {
  return { user, token };
};
const getMe = (token) => {
  return user;
};

const test = () => false;
//Auth


